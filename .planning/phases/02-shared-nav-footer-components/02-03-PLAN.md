---
phase: 02-shared-nav-footer-components
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - explorar/_nav-footer-test.html
autonomous: false
must_haves:
  truths:
    - "`explorar/_nav-footer-test.html` exists and loads nav + footer correctly via `../components/components.js`"
    - "Nav links in the depth-1 test page have correct `href` values that point to the right pages (relative to repo root)"
    - "`aria-current='page'` is correctly applied on `explorar/_nav-footer-test.html` — specifically, the 'Explorar Jogos' link should match if the test page path includes `/explorar/`"
    - "Both test pages (root + depth-1) show identical nav and footer visual rendering — same styles, same layout, same wave, same stars"
    - "Keyboard-only navigation (Tab through all elements) works on both test pages — all interactive elements reachable, focus rings visible"
    - "Footer background is visually `#0B0F2E` on both test pages"
    - "CTA button gradient is visible on both test pages"
    - "Mobile hamburger menu works on both test pages at ≤640px"
  artifacts:
    - explorar/_nav-footer-test.html
  key_links:
    - "_nav-footer-test.html — root-level test page (created in Plan 02-02) for cross-comparison"
    - "components/components.js — must resolve paths correctly using document.currentScript.src"
    - ".planning/phases/02-shared-nav-footer-components/02-RESEARCH.md — Section 3 (Path Resolution)"
---

<objective>
Create a depth-1 test page in `explorar/` and verify that the component system works correctly across page depths, with complete validation of all Phase 2 success criteria.

Purpose: Path resolution is the hardest part of the component system. This plan proves that `components.js` correctly computes `BASE_URL` when loaded from a subdirectory, and that all 7 Phase 2 success criteria are met.

Output: `explorar/_nav-footer-test.html` + verified success criteria
</objective>

<execution_context>
@.github/skills/get-shit-done/workflows/execute-plan.md
@.github/skills/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-nav-footer-components/02-RESEARCH.md
@.planning/phases/02-shared-nav-footer-components/02-CONTEXT.md
@.planning/phases/02-shared-nav-footer-components/02-01-PLAN.md
@.planning/phases/02-shared-nav-footer-components/02-02-PLAN.md
@components/nav.html
@components/footer.html
@components/components.css
@components/components.js
@_nav-footer-test.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create depth-1 test page `explorar/_nav-footer-test.html`</name>
  <files>explorar/_nav-footer-test.html</files>
  <action>
Create `explorar/_nav-footer-test.html` — a test page at directory depth 1 that verifies the component system works from a subdirectory.

**Key difference from root test page:** All paths use `../` prefix since we're one level deep.

**Exact HTML:**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Nav &amp; Footer Test — Depth 1 (explorar/) | Meu Planetinha</title>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fredoka+One&family=Nunito:wght@400;700&family=Press+Start+2P&display=swap" rel="stylesheet">

  <!-- CSS load order (depth-1: all paths prefixed with ../) -->
  <link rel="stylesheet" href="../estilos/reset.css">
  <link rel="stylesheet" href="../estilos/base.css">
  <link rel="stylesheet" href="../estilos/layout.css">
  <link rel="stylesheet" href="../estilos/componentes.css">
  <link rel="stylesheet" href="../components/components.css">
</head>
<body>
  <!-- Nav placeholder -->
  <div data-component="nav">
    <noscript>
      <nav class="site-nav" aria-label="Navegação principal">
        <div class="nav-container">
          <a class="nav-logo" href="../index.html">🪐 Meu Planetinha</a>
          <div class="nav-links">
            <a href="../explorar/explorar.html">Explorar Jogos</a>
            <a href="../sobre_nos/sobre_nos.html">Sobre Nós</a>
          </div>
        </div>
      </nav>
    </noscript>
  </div>

  <!-- Main content -->
  <main id="main-content" class="container" style="padding: var(--espaco-3xl) var(--espaco-md);">
    <h1>Test: Nav &amp; Footer from Depth 1</h1>
    <p><strong>Depth:</strong> 1 (<code>/explorar/</code>)</p>
    <p><strong>Script src:</strong> <code>../components/components.js</code></p>

    <h2>Path Resolution Checks</h2>
    <ul style="list-style: disc inside; line-height: 2;">
      <li><strong>BASE_URL should NOT include <code>explorar/</code></strong> — it should end at the repo root</li>
      <li>Nav link "Explorar Jogos" → <code>{BASE_URL}explorar/explorar.html</code></li>
      <li>Nav link "Sobre Nós" → <code>{BASE_URL}sobre_nos/sobre_nos.html</code></li>
      <li>Logo link → <code>{BASE_URL}index.html</code></li>
      <li>Footer "Contato" → <code>mailto:contato@meuplanetinha.com</code> (no BASE_URL prefix)</li>
    </ul>

    <h2>Active Page Check</h2>
    <p>This test page is at <code>/explorar/_nav-footer-test.html</code>. It is NOT <code>explorar/explorar.html</code>, so <strong>no nav link should have <code>aria-current="page"</code></strong> on this page.</p>
    <p>To test active-page detection properly, the real Explorar page (when rebuilt in Phase 7) will need to be at <code>explorar/explorar.html</code>.</p>

    <h2>Visual Checks</h2>
    <ul style="list-style: disc inside; line-height: 2;">
      <li>Nav bar looks identical to the root test page</li>
      <li>Footer looks identical to the root test page</li>
      <li>CTA button has yellow-to-orange gradient</li>
      <li>Footer has wavy top border and dark nav background</li>
      <li>Stars are visible in the footer area</li>
      <li>Mobile hamburger works at ≤640px</li>
    </ul>

    <h2>Computed BASE_URL</h2>
    <p id="base-url-display" style="font-family: monospace; background: rgba(255,255,255,0.1); padding: var(--espaco-sm); border-radius: var(--borda-raio-md);">
      (will be filled by JS below)
    </p>
  </main>

  <!-- Footer placeholder -->
  <div data-component="footer"></div>

  <!-- Component loader (depth-1 path: ../) -->
  <script src="../components/components.js"></script>

  <!-- Debug: show computed BASE_URL -->
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      var display = document.getElementById('base-url-display');
      if (display && typeof BASE_URL !== 'undefined') {
        display.textContent = 'BASE_URL = "' + BASE_URL + '"';
      }
    });
  </script>
</body>
</html>
```

**Critical notes:**
- ALL asset paths use `../` prefix: `../estilos/reset.css`, `../components/components.css`, `../components/components.js`
- The `<script src="../components/components.js">` tag has NO `defer`, `async`, or `type="module"`
- The browser resolves `../components/components.js` to an absolute URL in `document.currentScript.src`. Then `components.js` strips `components/components.js` to get the base URL — which should NOT include `explorar/`.
  </action>
  <verify>
1. Open `explorar/_nav-footer-test.html` via a local HTTP server
2. Nav bar renders — logo, links, CTA button all visible
3. Footer renders — wave, logo, tagline, Contato, stars all visible
4. Inspect nav links in DevTools: `href` values should use the correct base path (NOT double `explorar/explorar/`)
5. The `BASE_URL` debug display should show a path ending at the repo root, NOT including `explorar/`
6. No nav link has `aria-current="page"` (this page is not a nav target)
7. Mobile hamburger works at ≤640px
8. No console errors
  </verify>
  <done>
`explorar/_nav-footer-test.html` exists at depth 1, loads nav + footer via `../components/components.js`, all paths resolve correctly, and the visual output is identical to the root test page.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify all Phase 2 success criteria</name>
  <files>none (verification only)</files>
  <what-built>
Phase 2 component system: `components/nav.html`, `components/footer.html`, `components/components.css`, `components/components.js`, root test page `_nav-footer-test.html`, and depth-1 test page `explorar/_nav-footer-test.html`.
  </what-built>
  <how-to-verify>
Systematically verify all 7 Phase 2 success criteria. Serve the project via a local HTTP server and test both pages.

**Start a local server:**
```bash
python -m http.server 8000
```
Or use VS Code Live Server.

**Criterion 1: Files exist**
Verify these files exist and are non-empty:
- `components/nav.html`
- `components/footer.html`
- `components/components.js`
(Also verify `components/components.css` exists — not listed in the original criteria but essential.)

**Criterion 2: Root depth test**
Open `http://localhost:8000/_nav-footer-test.html`:
- Nav loads: logo visible, "Explorar Jogos" link visible, "Sobre Nós" link visible, CTA button visible
- Footer loads: wave visible, logo visible, tagline visible, "Contato" link visible
- No console errors

**Criterion 3: Depth-1 test**
Open `http://localhost:8000/explorar/_nav-footer-test.html`:
- Same checks as Criterion 2
- Compare `BASE_URL` display between both pages — they should resolve to the same base path
- Inspect link `href` values in DevTools — they should point to valid pages (not broken paths)

**Criterion 4: `aria-current="page"`**
This requires testing on a page that IS a nav target. Since we haven't rebuilt the real pages yet, the test pages SHOULD NOT have `aria-current` set (they're not nav targets). Verify:
- On `_nav-footer-test.html` (root): no link has `aria-current="page"` ✓
- On `explorar/_nav-footer-test.html`: no link has `aria-current="page"` ✓
- Code inspection: read `components.js` and confirm the `markActivePage` function compares normalized `location.pathname` against each link's resolved `href` pathname. This logic will work correctly once real pages exist at `explorar/explorar.html` and `sobre_nos/sobre_nos.html`.

If you want to do a live test of `aria-current`: temporarily create a minimal `explorar/explorar.html` that loads `components.js`, open it, and verify the "Explorar Jogos" link gets `aria-current="page"`. Then delete the temporary file. (Optional — code inspection is sufficient for this plan.)

**Criterion 5: Fallback on fetch failure / JS disabled**
Verify fallback behavior:
- In DevTools Network tab, block the fetch for `components/nav.html` (right-click → Block request URL) → page loads without errors, **minimal fallback nav renders** with Home, Explorar Jogos, Sobre Nós links ✓
- Disable JavaScript in browser settings → `<noscript>` fallback nav renders inside `[data-component="nav"]` with hardcoded links ✓

**Criterion 6: Footer visual check**
- Footer background is dark navy (`#0B0F2E`) — inspect computed style in DevTools
- Tagline says "Explore o universo do aprendizado!"
- "Contato" link is present
- Logo is present
- Wavy top border is visible (the clip-path wave separating footer from main content)

**Criterion 7: Keyboard navigation**
- Press Tab from the top of the page:
  1. First Tab: skip-link becomes visible ("Pular para o conteúdo")
  2. Pressing Enter on skip-link: focus jumps to `#main-content`
  3. Continue Tab: logo link → Explorar Jogos link → Sobre Nós link → CTA button → main content links → footer links
  4. All focused elements have a visible white outline (`:focus-visible` style from `base.css`)
- At ≤640px viewport (mobile):
  1. Tab reaches the hamburger button
  2. Enter/Space opens the overlay
  3. Focus moves to close button
  4. Tab cycles through overlay links + CTA
  5. Escape closes overlay, focus returns to hamburger

**If any criterion fails:** Note the failure for the developer to fix.
  </how-to-verify>
  <resume-signal>All 7 success criteria confirmed visually and in DevTools. Any failures have been noted for fixing.</resume-signal>
</task>

</tasks>

<verification>
Final cross-verification:
1. Both test pages (`_nav-footer-test.html` and `explorar/_nav-footer-test.html`) load without console errors
2. Both show identical nav and footer visual rendering
3. `BASE_URL` on both pages resolves to the same repo root
4. All nav link `href` values are identical on both pages (comparing absolute URLs)
5. Mobile menu works on both pages
6. Keyboard navigation works on both pages
7. Skip-to-content link works on both pages
8. Footer wave, stars, and dark background are visible on both pages
</verification>

<success_criteria>
- `explorar/_nav-footer-test.html` exists and loads nav + footer correctly from depth 1
- Path resolution works: `BASE_URL` is identical whether loaded from root or depth 1
- All 7 Phase 2 success criteria verified
- No regression: root test page still works after any fixes made during verification
- Phase 2 is fully complete and ready for downstream phases
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-nav-footer-components/02-03-SUMMARY.md`
</output>
