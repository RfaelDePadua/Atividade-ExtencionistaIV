---
phase: 14-3d-orbital-carousel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - estilos/base.css
  - index.html
autonomous: true

must_haves:
  truths:
    - "base.css contains all new 3D orbit tokens (--orbita-rotacao-duracao, --orbita-easing, --orbita-opacidade-fundo, --orbita-blur-fundo, --orbita-angulo-passo, --z-orbit-arrows)"
    - "base.css token --perspectiva-3d is 700px, --orbita-raio is 300px, --orbita-inclinacao is 25deg"
    - "index.html carousel section contains .carousel-scene wrapper with perspective and .carousel-orbit replacing .carousel-track"
    - "index.html links to carousel-3d.css instead of carousel.css"
    - "index.html links to carousel-3d.js instead of carousel.js"
    - "All 5 planet cards have data-orbit-zone attribute"
    - "Arrows use minimal chevron characters (‹ ›) and are inside .carousel-scene but outside .carousel-orbit"
    - "Dot nav is outside .carousel-scene"
    - "All v1 ARIA attributes preserved: aria-roledescription=carousel, aria-live, aria-label, role=group, aria-roledescription=slide"
    - "NO overflow:hidden on .carousel-scene, .carousel-orbit, or .container ancestor"
  artifacts:
    - path: "estilos/base.css"
      provides: "Updated 3D orbit design tokens"
      contains: "--orbita-rotacao-duracao"
    - path: "index.html"
      provides: "Restructured 3D carousel HTML"
      contains: "carousel-scene"
  key_links:
    - from: "index.html"
      to: "estilos/carousel-3d.css"
      via: "stylesheet link"
      pattern: "carousel-3d\\.css"
    - from: "index.html"
      to: "scripts/carousel-3d.js"
      via: "script tag"
      pattern: "carousel-3d\\.js"
---

<objective>
Update base.css design tokens for 3D orbit parameters and restructure the index.html carousel HTML from v1 flat layout to v2 3D orbital container model.

Purpose: Establish the token foundation and DOM structure that carousel-3d.css and carousel-3d.js will target. Without correct HTML structure and tokens, the 3D CSS and JS cannot function.
Output: Updated `estilos/base.css` with corrected/new tokens; restructured `index.html` carousel section with 3-layer container model.
</objective>

<execution_context>
@.github/skills/get-shit-done/workflows/execute-plan.md
@.github/skills/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

[Source files:]
@estilos/base.css
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update base.css 3D & animation tokens</name>
  <files>estilos/base.css</files>
  <action>
In `estilos/base.css`, locate the `3D & ANIMATION TOKENS (v2.0)` section inside `:root`. Apply these changes:

1. **Update existing tokens:**
   - `--perspectiva-3d: 1200px` → `--perspectiva-3d: 700px`
   - `--orbita-raio: 220px` → `--orbita-raio: 300px`
   - `--orbita-inclinacao: 62deg` → `--orbita-inclinacao: 25deg`

2. **Add new tokens** immediately after `--orbita-inclinacao`:
```css
  --orbita-rotacao-duracao: 600ms;
  --orbita-easing:          ease-out;
  --orbita-opacidade-fundo: 0.3;
  --orbita-blur-fundo:      2.5px;
  --orbita-angulo-passo:    72deg;
```

3. **Add z-index token** in the `Z-index layers` sub-section, after `--z-carousel: 10`:
```css
  --z-orbit-arrows:         15;
```

4. **Add responsive overrides.** After the existing `@media (prefers-reduced-motion: reduce)` block but BEFORE the `::selection` rule, add two new media queries:

```css
@media (max-width: 768px) {
  :root {
    --orbita-raio: 200px;
    --perspectiva-3d: 600px;
  }
}

@media (max-width: 480px) {
  :root {
    --orbita-raio: 140px;
    --perspectiva-3d: 500px;
  }
}
```

**Do NOT** change any other tokens or base styles. Leave all palette, typography, spacing, shadow, border, and transition tokens untouched.
  </action>
  <verify>
Open `estilos/base.css` and confirm:
- `--perspectiva-3d` value is `700px`
- `--orbita-raio` value is `300px`
- `--orbita-inclinacao` value is `25deg`
- Six new tokens exist: `--orbita-rotacao-duracao`, `--orbita-easing`, `--orbita-opacidade-fundo`, `--orbita-blur-fundo`, `--orbita-angulo-passo`, `--z-orbit-arrows`
- Two responsive media queries override `--orbita-raio` and `--perspectiva-3d` at 768px and 480px breakpoints
- No syntax errors in the file (check for unclosed braces/missing semicolons)
  </verify>
  <done>All 3 token updates applied; 6 new tokens added; 2 responsive breakpoints added. File parses without CSS errors.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure index.html carousel to 3D container model</name>
  <files>index.html</files>
  <action>
In `index.html`, make the following changes to transform the v1 carousel into the v2 3D orbital structure:

**A. Update CSS link (line ~26):**
Change `estilos/carousel.css` → `estilos/carousel-3d.css`:
```html
  <link rel="stylesheet" href="estilos/carousel-3d.css">
```

**B. Update JS script tag (near end of file, currently `scripts/carousel.js`):**
Change to:
```html
  <script src="scripts/carousel-3d.js"></script>
```

**C. Restructure the carousel section.** Replace the entire content between `<section id="carousel" ...>` and `</section>` with the new 3D structure. The section tag itself and its attributes stay identical.

New structure inside `<section id="carousel" role="region" aria-label="Carrossel de planetas" data-active-planet="calculon" tabindex="0">`:

```html
      <div class="container">

        <!-- 3D Perspective Scene -->
        <div class="carousel-scene">

          <!-- 3D Orbit Ring (rotates as a unit via --orbit-angle) -->
          <div class="carousel-orbit" aria-roledescription="carousel" aria-label="Planetas temáticos">

            <!-- Planet: Calculon -->
            <div class="planet-card" data-planet="calculon" data-orbit-zone="center" role="group" aria-roledescription="slide" aria-label="Calculon — Matemática">
              <div class="planet-sphere planet-sphere--calculon"></div>
              <div class="planet-info">
                <span class="planet-name">Calculon</span>
                <span class="planet-subject">Matemática</span>
              </div>
            </div>

            <!-- Planet: Letrion -->
            <div class="planet-card" data-planet="letrion" data-orbit-zone="side" role="group" aria-roledescription="slide" aria-label="Letrion — Português">
              <div class="planet-sphere planet-sphere--letrion"></div>
              <div class="planet-info">
                <span class="planet-name">Letrion</span>
                <span class="planet-subject">Português</span>
              </div>
            </div>

            <!-- Planet: Naturox -->
            <div class="planet-card" data-planet="naturox" data-orbit-zone="back" role="group" aria-roledescription="slide" aria-label="Naturox — Ciências">
              <div class="planet-sphere planet-sphere--naturox"></div>
              <div class="planet-info">
                <span class="planet-name">Naturox</span>
                <span class="planet-subject">Ciências</span>
              </div>
            </div>

            <!-- Planet: Terramund -->
            <div class="planet-card" data-planet="terramund" data-orbit-zone="back" role="group" aria-roledescription="slide" aria-label="Terramund — Geografia">
              <div class="planet-sphere planet-sphere--terramund"></div>
              <div class="planet-info">
                <span class="planet-name">Terramund</span>
                <span class="planet-subject">Geografia</span>
              </div>
            </div>

            <!-- Planet: Globish -->
            <div class="planet-card" data-planet="globish" data-orbit-zone="side" role="group" aria-roledescription="slide" aria-label="Globish — Inglês">
              <div class="planet-sphere planet-sphere--globish"></div>
              <div class="planet-info">
                <span class="planet-name">Globish</span>
                <span class="planet-subject">Inglês</span>
              </div>
            </div>

          </div><!-- /.carousel-orbit -->

          <!-- Navigation arrows (inside scene for positioning, outside orbit for no 3D transform) -->
          <button class="carousel-arrow carousel-arrow--prev" aria-label="Planeta anterior" type="button" tabindex="-1">
            <span class="carousel-arrow-icon" aria-hidden="true">&#8249;</span>
          </button>
          <button class="carousel-arrow carousel-arrow--next" aria-label="Próximo planeta" type="button" tabindex="-1">
            <span class="carousel-arrow-icon" aria-hidden="true">&#8250;</span>
          </button>

        </div><!-- /.carousel-scene -->

        <!-- Navigation dots (OUTSIDE scene — flat 2D, below 3D space) -->
        <nav class="carousel-dots" aria-label="Navegação do carrossel">
          <button class="carousel-dot" data-index="0" aria-label="Ir para Calculon" type="button" tabindex="-1"></button>
          <button class="carousel-dot" data-index="1" aria-label="Ir para Letrion" type="button" tabindex="-1"></button>
          <button class="carousel-dot" data-index="2" aria-label="Ir para Naturox" type="button" tabindex="-1"></button>
          <button class="carousel-dot" data-index="3" aria-label="Ir para Terramund" type="button" tabindex="-1"></button>
          <button class="carousel-dot" data-index="4" aria-label="Ir para Globish" type="button" tabindex="-1"></button>
        </nav>

        <!-- Screen reader live region (populated by JS) -->
        <div class="carousel-announcer visually-hidden" aria-live="polite" aria-atomic="true"></div>

        <!-- Sparkle accents (ANIM-03) — decorative twinkles around carousel -->
        <span class="sparkle" style="top:12%;left:8%;animation-delay:-0.0s;animation-duration:2.0s" aria-hidden="true"></span>
        <span class="sparkle" style="top:68%;left:15%;animation-delay:-0.4s;animation-duration:1.6s" aria-hidden="true"></span>
        <span class="sparkle" style="top:25%;left:85%;animation-delay:-1.1s;animation-duration:2.2s" aria-hidden="true"></span>
        <span class="sparkle" style="top:72%;left:90%;animation-delay:-0.7s;animation-duration:1.8s" aria-hidden="true"></span>
        <span class="sparkle" style="top:5%;left:50%;animation-delay:-1.5s;animation-duration:2.4s" aria-hidden="true"></span>
        <span class="sparkle" style="top:55%;left:35%;animation-delay:-0.3s;animation-duration:1.5s" aria-hidden="true"></span>
        <span class="sparkle" style="top:40%;left:70%;animation-delay:-1.9s;animation-duration:2.1s" aria-hidden="true"></span>

      </div><!-- /.container -->
```

**Key structural changes from v1:**
- `.carousel-track` renamed to `.carousel-orbit` (the 3D rotating ring)
- New `.carousel-scene` wrapper added around orbit + arrows (perspective container)
- Each planet card gets `data-orbit-zone` attribute (initial values: calculon="center", letrion="side", naturox="back", terramund="back", globish="side" — JS will update these dynamically)
- Arrows moved INSIDE `.carousel-scene` but OUTSIDE `.carousel-orbit` — positioned in 2D within the perspective scene
- Arrow icons changed from `&#9664;`/`&#9654;` (solid triangles ◀▶) to `&#8249;`/`&#8250;` (single angle quotes ‹›)
- Dots remain OUTSIDE `.carousel-scene` (flat 2D below the orbit)
- All ARIA attributes preserved exactly: `role="region"`, `aria-label`, `aria-roledescription="carousel"`, `aria-roledescription="slide"`, `role="group"`, `aria-live="polite"`, `aria-atomic="true"`, `tabindex` values
- Sparkles and announcer remain in `.container` outside `.carousel-scene`

**Do NOT change:**
- The `<section id="carousel" ...>` opening tag or its attributes
- Any content outside the carousel section
- The sparkle elements or their inline styles
- The announcer div
  </action>
  <verify>
Open `index.html` and confirm:
- `<link>` tag references `estilos/carousel-3d.css` (not `carousel.css`)
- `<script>` tag references `scripts/carousel-3d.js` (not `carousel.js`)
- `.carousel-scene` wraps `.carousel-orbit` + arrow buttons
- `.carousel-orbit` has `aria-roledescription="carousel"` (moved from old `.carousel-track`)
- 5 planet cards inside `.carousel-orbit`, each with `data-orbit-zone` attribute
- Arrow buttons inside `.carousel-scene`, outside `.carousel-orbit`, using `&#8249;`/`&#8250;`
- `.carousel-dots` nav is outside `.carousel-scene`
- `.carousel-announcer` and sparkles are in `.container`, outside `.carousel-scene`
- No `overflow: hidden` anywhere in the carousel section
- HTML is well-formed (all tags properly closed)
  </verify>
  <done>index.html has 3-layer 3D container model (.carousel-scene > .carousel-orbit > .planet-card), all ARIA preserved, CSS/JS links updated to carousel-3d variants, arrow icons are minimal chevrons.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `estilos/base.css` has 3 updated tokens + 6 new tokens + 2 responsive breakpoints
- [ ] `index.html` references `carousel-3d.css` and `carousel-3d.js`
- [ ] `index.html` has `.carousel-scene` > `.carousel-orbit` > `.planet-card` structure
- [ ] All 5 planet cards have `data-orbit-zone` attribute
- [ ] No `overflow: hidden` in carousel HTML
- [ ] All v1 ARIA attributes preserved
- [ ] Arrow icons are ‹ and › (not ◀ ▶)
- [ ] HTML is valid (no unclosed tags)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Token values: --perspectiva-3d: 700px, --orbita-raio: 300px, --orbita-inclinacao: 25deg
- 3D container model correctly nested in HTML
- ARIA accessibility attributes fully preserved from v1
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/14-3d-orbital-carousel/14-01-SUMMARY.md`
</output>
