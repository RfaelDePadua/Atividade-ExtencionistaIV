---
phase: 12-foundation-tokens-quick-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/nav.html
autonomous: true
must_haves:
  truths:
    - "Vanilla HTML only — no framework, no build step"
    - "Shared nav injected via fetch() in components/components.js — all pages load it"
    - "Desktop .nav-cta duplicates 'Explorar Jogos' from .nav-links — POLISH-01"
    - "Mobile overlay .nav-cta stays as-is (overlay hides .nav-links)"
    - "Main-site pages (index, explorar, sobre_nos, 404) do NOT load barra_superior.css — POLISH-02"
    - "Game pages under jogos/ use barra_superior.css; fix deferred to Phase 17 game shell"
  artifacts:
    - components/nav.html (desktop CTA text changed)
  key_links:
    - components/nav.html
    - components/components.js
    - components/components.css
    - estilos/barra_superior.css
---

<objective>
Eliminate the duplicate "Explorar Jogos" text in the navigation bar (POLISH-01) by
changing the desktop CTA button label to "Jogar!", and verify that no colored gradient
bar from barra_superior.css appears on any main-site page (POLISH-02).
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Change desktop .nav-cta text to "Jogar!" — POLISH-01</name>
  <files>components/nav.html</files>
  <action>
In `components/nav.html`, locate the DESKTOP CTA button (line 22, inside
`.nav-container`, NOT inside `#mobile-nav`):

**Old (line 22):**
```html
    <a class="nav-cta" data-href="explorar/explorar.html">Explorar Jogos</a>
```

**New:**
```html
    <a class="nav-cta" data-href="explorar/explorar.html">Jogar!</a>
```

Change ONLY the inner text of this one element. Do NOT modify:
- The `.nav-links` anchor that says "Explorar Jogos" (line 17) — keep it.
- The mobile overlay `.nav-cta` inside `#mobile-nav` (line 44) — keep "Explorar Jogos".
- Any attributes (`class`, `data-href`).

After this change the nav will read:
  Desktop: `🪐 Meu Planetinha  |  Explorar Jogos  ·  Sobre Nós  |  [Jogar!]`
  Mobile overlay: `Explorar Jogos  ·  Sobre Nós  ·  [Explorar Jogos]` (unchanged)
  </action>
  <verify>
1. Open any page (e.g. index.html) in a desktop-width browser (≥641px).
2. Confirm the nav shows "Explorar Jogos" exactly ONCE (in the link list), and a
   separate CTA button labeled "Jogar!".
3. Resize to ≤640px, open the hamburger menu, and confirm the overlay CTA still
   says "Explorar Jogos".
4. `grep -n "Explorar Jogos" components/nav.html` should return exactly 3 matches:
   one in `.nav-links`, one in `.nav-overlay-links`, one in mobile `.nav-cta`.
  </verify>
  <done>
Desktop nav renders "Explorar Jogos" exactly once; the CTA button reads "Jogar!".
Mobile overlay remains unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no colored bar on main-site pages — POLISH-02</name>
  <files>components/nav.html</files>
  <action>
This is a VERIFICATION-ONLY task — no file edits expected.

Check that NONE of these four files link to `estilos/barra_superior.css`:
  - index.html
  - explorar/explorar.html
  - sobre_nos/sobre_nos.html
  - 404.html

Run from repo root:
```bash
grep -l "barra_superior" index.html explorar/explorar.html sobre_nos/sobre_nos.html 404.html
```

Expected: NO output (no matches). If any file does reference `barra_superior.css`:
  1. Remove the `<link>` tag that loads it.
  2. Confirm the page still renders correctly without a colored gradient header bar.

If grep returns nothing — no action needed; POLISH-02 is already satisfied on main pages.

NOTE: `jogos/Contando_Estrelas/index.html` and `jogos/Jogo_de_Silaba/index.html` DO
load barra_superior.css — this is expected and will be resolved in Phase 17.
  </action>
  <verify>
1. `grep -rl "barra_superior" index.html explorar/explorar.html sobre_nos/sobre_nos.html 404.html` returns empty.
2. Open each of the four main pages in a browser and visually confirm there is no
   colored gradient bar (old v0 header) above or below the standard nav.
  </verify>
  <done>
No main-site page (index, explorar, sobre_nos, 404) loads barra_superior.css or displays
a colored gradient header bar. POLISH-02 verified.
  </done>
</task>
</tasks>

<verification>
1. Header renders "Explorar Jogos" exactly once on every main page at desktop width.
2. CTA button in desktop nav reads "Jogar!" — clearly differentiated from the nav link.
3. No colored gradient bar from barra_superior.css appears on index, explorar, sobre_nos, or 404.
4. Mobile hamburger menu still shows "Explorar Jogos" CTA — no regression.
</verification>

<success_criteria>
- "Explorar Jogos" appears exactly once in the rendered desktop nav on every page.
- No colored bar visible on any main-site page (POLISH-02).
- Mobile overlay CTA unchanged.
</success_criteria>
