---
phase: 12-foundation-tokens-quick-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - estilos/cards.css
  - scripts/games.js
autonomous: true
must_haves:
  truths:
    - "Vanilla HTML/CSS/JS only — no build tools, no bundlers"
    - "scripts/games.js renders cards via GAME_DATA array and createCard() function"
    - "Currently only 2 games in GAME_DATA — need an 'Em Breve' placeholder as 3rd card"
    - "Grid uses auto-fill — needs explicit 3-col on desktop, 2-col on tablet/mobile"
    - ".game-card needs flex column layout so button pushes to bottom — POLISH-04"
    - "#jogos section padding is var(--espaco-2xl) 0 (48px) — verify ≥24px above footer"
    - "body.page-home scoping pattern for homepage-only overrides"
    - "Zero new dependencies"
  artifacts:
    - estilos/cards.css (grid columns + card flex + spacing)
    - scripts/games.js ("Em Breve" placeholder card)
  key_links:
    - estilos/cards.css
    - scripts/games.js
    - index.html
    - estilos/pages/homepage.css
---

<objective>
Fix the homepage game-card grid to display 3 columns on desktop with a visible
"Em Breve" placeholder as the third card (POLISH-03), make the "Jogar!" button stick
to the bottom of every card regardless of description length (POLISH-04), and ensure at
least 24px of visible space between the cards section and the footer (POLISH-05).
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Fix card grid columns + card flex layout — POLISH-03 grid + POLISH-04 + POLISH-05</name>
  <files>estilos/cards.css</files>
  <action>
Make THREE changes in `estilos/cards.css`:

**Change A — Grid columns (POLISH-03):**
Locate the `.game-grid` rule (around line 73):
```css
.game-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: var(--espaco-xl);
  justify-items: center;
}
```

Replace with:
```css
.game-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--espaco-xl);
  justify-items: center;
}
```

This enforces exactly 3 columns on desktop. The existing `@media (max-width: 640px)`
rule already sets `grid-template-columns: 1fr` for mobile. We need an intermediate
breakpoint for 2-column tablet layout.

**Change B — Card flex structure (POLISH-04):**
Locate the `.game-card` rule (around line 82):
```css
.game-card {
  width: 100%;
  max-width: 340px;
  border-radius: var(--borda-raio-xl);
  overflow: hidden;
  box-shadow: var(--sombra-md);
  transition:
    transform var(--duracao-rapida) var(--easing-padrao),
    box-shadow var(--duracao-rapida) var(--easing-padrao);
  /* Default gradient fallback */
  background: linear-gradient(
    135deg,
    rgba(255, 255, 255, 0.12),
    rgba(255, 255, 255, 0.05)
  );
}
```

Add these three properties at the end of this rule (before the closing `}`),
after the `background` property:
```css
  /* Flex column so card body can grow and push button to bottom */
  display: flex;
  flex-direction: column;
  height: 100%;
```

Then locate the `.game-card-body` rule (around line 107):
```css
.game-card-body {
  padding: var(--espaco-2xl) var(--espaco-lg) var(--espaco-xl);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--espaco-md);
  text-align: center;
}
```

Add this property at the end of the rule (before closing `}`):
```css
  flex-grow: 1;
```

Then locate the `.game-card .game-card-btn` rule (around line 155):
```css
.game-card .game-card-btn {
  margin-top: var(--espaco-sm);
  min-width: 140px;
  background: rgba(255, 255, 255, 0.95);
  color: var(--cor-texto-escuro);
  box-shadow: var(--sombra-sm);
}
```

Change `margin-top: var(--espaco-sm);` to `margin-top: auto;`:
```css
.game-card .game-card-btn {
  margin-top: auto;
  min-width: 140px;
  background: rgba(255, 255, 255, 0.95);
  color: var(--cor-texto-escuro);
  box-shadow: var(--sombra-sm);
}
```

`margin-top: auto` in a flex column pushes the button to the bottom of the card body,
regardless of how much description text the cards above it contain.

**Change C — Tablet 2-column breakpoint + spacing (POLISH-05):**
Locate the existing `@media (max-width: 640px)` responsive block at the bottom of the
file (around line 287):
```css
@media (max-width: 640px) {
  .game-grid {
    grid-template-columns: 1fr;
    gap: var(--espaco-lg);
  }
  ...
}
```

BEFORE this block, insert a new tablet breakpoint:
```css
/* Tablet: 2-column grid */
@media (max-width: 960px) and (min-width: 641px) {
  .game-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}
```

For POLISH-05 spacing: The `#jogos` section already has `padding: var(--espaco-2xl) 0`
(= 48px top and bottom), which exceeds the 24px minimum. However, to be safe and
explicit, change the `#jogos` rule (around line 17):

**Old:**
```css
#jogos {
  padding: var(--espaco-2xl) 0;
}
```

**New:**
```css
#jogos {
  padding: var(--espaco-2xl) 0;
  padding-bottom: var(--espaco-3xl); /* 64px — ensure ≥24px visible before footer */
}
```

This guarantees 64px of bottom padding, well above the 24px minimum.

**Change D — "Em Breve" placeholder card styles (POLISH-03 visual):**
After the `.game-card:hover` rule (around line 99), insert:

```css
/* ===========================================
   "EM BREVE" PLACEHOLDER CARD
   =========================================== */

.game-card--placeholder {
  border: 2px dashed rgba(255, 255, 255, 0.25);
  background: linear-gradient(
    135deg,
    rgba(255, 255, 255, 0.06),
    rgba(255, 255, 255, 0.02)
  );
  cursor: default;
}

.game-card--placeholder:hover {
  transform: none;
  box-shadow: var(--sombra-md);
}

.game-card-placeholder-icon {
  font-size: var(--texto-4xl);
  display: block;
  opacity: 0.6;
}

.game-card--placeholder .game-card-name {
  color: rgba(255, 255, 255, 0.6);
}

.game-card--placeholder .game-card-subject {
  color: rgba(255, 255, 255, 0.4);
}
```
  </action>
  <verify>
1. Open `index.html` at desktop width (≥961px):
   - Confirm exactly 3 columns in the card grid.
   - Confirm "Jogar!" buttons are visually aligned at the bottom of each card.
2. Resize to tablet width (641px–960px): Confirm 2-column layout.
3. Resize to mobile (≤640px): Confirm 1-column layout (existing behavior).
4. Scroll to the bottom of the cards section: confirm ≥24px of visible space
   between the last card row and the footer.
5. No CSS parse errors.
  </verify>
  <done>
Card grid shows 3 columns on desktop, 2 on tablet, 1 on mobile. "Jogar!" button
is flush-bottom on every card. At least 24px visible space before footer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "Em Breve" placeholder card — POLISH-03 placeholder</name>
  <files>scripts/games.js</files>
  <action>
In `scripts/games.js`, locate the `renderCards` function (around line 99):

```javascript
  function renderCards(options) {
    options = options || {};
    var gridId = options.gridId || 'game-grid';
    var basePath = options.basePath || '';
    var grid = document.getElementById(gridId);
    if (!grid) return;

    var html = '';
    for (var i = 0; i < GAME_DATA.length; i++) {
      html += createCard(GAME_DATA[i], basePath);
    }
    grid.innerHTML = html;
  }
```

Replace with:
```javascript
  /**
   * Build the "Em Breve" placeholder card HTML.
   * Shown after all real game cards to preview upcoming content.
   */
  function createPlaceholderCard() {
    return (
      '<article class="game-card game-card--placeholder" aria-label="Em Breve — novo jogo será adicionado">' +
        '<div class="game-card-body">' +
          '<span class="game-card-placeholder-icon" aria-hidden="true">🚀</span>' +
          '<h3 class="game-card-name">Em Breve</h3>' +
          '<span class="game-card-subject">Novo jogo a caminho!</span>' +
        '</div>' +
      '</article>'
    );
  }

  /**
   * Render all game cards into a target grid container.
   * @param {Object} [options] - Optional configuration.
   * @param {string} [options.gridId='game-grid'] - ID of the target grid element.
   * @param {string} [options.basePath=''] - Path prefix for game hrefs (e.g. '../' from depth-1 pages).
   */
  function renderCards(options) {
    options = options || {};
    var gridId = options.gridId || 'game-grid';
    var basePath = options.basePath || '';
    var grid = document.getElementById(gridId);
    if (!grid) return;

    var html = '';
    for (var i = 0; i < GAME_DATA.length; i++) {
      html += createCard(GAME_DATA[i], basePath);
    }
    /* Append "Em Breve" placeholder as the last card */
    html += createPlaceholderCard();
    grid.innerHTML = html;
  }
```

Also update the `filterCards` function to skip the placeholder card during filtering.
In the `filterCards` function (around line 128), locate:

```javascript
    for (var i = 0; i < cards.length; i++) {
      var cardPlanet = cards[i].getAttribute('data-planeta');
      if (!planet || cardPlanet === planet) {
        cards[i].removeAttribute('hidden');
        visibleCount++;
      } else {
        cards[i].setAttribute('hidden', '');
      }
    }
```

Replace with:
```javascript
    for (var i = 0; i < cards.length; i++) {
      /* Skip placeholder card — it's always visible */
      if (cards[i].classList.contains('game-card--placeholder')) {
        visibleCount++;
        continue;
      }
      var cardPlanet = cards[i].getAttribute('data-planeta');
      if (!planet || cardPlanet === planet) {
        cards[i].removeAttribute('hidden');
        visibleCount++;
      } else {
        cards[i].setAttribute('hidden', '');
      }
    }
```

This ensures the placeholder is never hidden by planet filters and always counts
toward the visible count (preventing false empty-state triggers).
  </action>
  <verify>
1. Open `index.html` at desktop width.
2. Confirm 3 cards visible: "Contando Estrelas", "Jogo de Sílaba", and an
   "Em Breve" placeholder with a 🚀 icon and dashed border.
3. Click a planet in the carousel to filter — the "Em Breve" card should remain
   visible alongside any matching game cards.
4. Click "Mostrar todos" — all 3 cards visible again.
5. Open `explorar/explorar.html` — confirm the "Em Breve" card also appears there.
  </verify>
  <done>
"Em Breve" placeholder card renders as the third card in the grid on all pages
that use `renderCards()`. It has a dashed border, rocket emoji, and muted styling.
Filtering does not hide it.
  </done>
</task>
</tasks>

<verification>
1. Index page at desktop width shows exactly 3 columns: 2 real games + 1 "Em Breve" placeholder.
2. All "Jogar!" buttons are flush-bottom across cards of varying content height.
3. Tablet (641–960px) shows 2 columns; mobile (≤640px) shows 1 column.
4. At least 24px (actually 64px) visible space between last card row and footer.
5. "Em Breve" card has dashed border, muted colors, 🚀 icon — visually distinct from real cards.
6. Planet filtering keeps placeholder visible; empty state only triggers when 0 real
   games + 0 visible (which won't happen since placeholder always counts).
</verification>

<success_criteria>
- Homepage game cards display in a 3-column grid (2-col on tablet, 1-col on mobile).
- Visible "Em Breve" placeholder appears as the third card.
- "Jogar!" button sits flush at the bottom of every game card regardless of description length.
- At least 24px of visible space separates the cards section from the footer on index.html.
- No JS or CSS errors; no visual regressions.
</success_criteria>
